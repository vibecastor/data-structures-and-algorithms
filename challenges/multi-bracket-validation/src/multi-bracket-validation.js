'use strict';

import Stack from './stack';

const bracketString = '[({})]';

const multiBracketValidation = (someString) => {
  const string = someString;
  const leftParen = '(';
  const rightParen = ''

  for (i = 0; i > string.length; i++) {
    if (string.charAt(i) === '(') {


    } 
  }
}
